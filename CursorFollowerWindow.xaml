<Window
  x:Class="KeyboardLanguageIndicator.CursorFollowerWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  WindowStyle="None"
  AllowsTransparency="True"
  Background="Transparent"
  Topmost="True"
  ShowInTaskbar="False"
  SizeToContent="WidthAndHeight"
  ResizeMode="NoResize"
  Loaded="Window_Loaded"
>
  <!-- Dimensions are calculated dynamically based on monitor resolution -->
  <Border x:Name="FollowerBorder" CornerRadius="2">
    <Border.Effect>
      <DropShadowEffect BlurRadius="4" ShadowDepth="0.5" Opacity="0.2" />
    </Border.Effect>
    <TextBlock
      x:Name="LanguageText"
      FontWeight="Normal"
      HorizontalAlignment="Center"
      VerticalAlignment="Center"
    />
  </Border>
</Window>
